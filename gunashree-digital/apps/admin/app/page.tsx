'use client'; import {useEffect,useState} from 'react';
const API=process.env.NEXT_PUBLIC_API_URL||'';
export default function Dashboard(){const [s,setS]=useState<any>({users:0,templates:0,designs:0,media:0}); useEffect(()=>{const t=localStorage.getItem('gd_token'); if(t)fetch(API+'/api/admin/stats',{headers:{Authorization:'Bearer '+t}}).then(r=>r.ok?r.json():null).then(x=>x&&setS(x))},[]); return <><div className="top"><div><h1>Dashboard</h1><p>Gunashree Digital — Admin Control Center</p></div></div><div className="grid">{[['Users',s.users],['Templates',s.templates],['Designs',s.designs],['Media',s.media]].map(x=><div className="card" key={x[0] as string}><div>{x[0]}</div><div className="metric">{x[1]}</div></div>)}</div><div className="card" style={{marginTop:16}}><h2>System flow</h2><p>Admin uploads template → defines editable fields → publishes → mobile app fetches template → user edits → saves design → exports/shares.</p></div></>}
