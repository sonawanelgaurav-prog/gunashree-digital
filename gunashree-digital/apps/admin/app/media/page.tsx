'use client'; import {useEffect,useState} from 'react'; const API=process.env.NEXT_PUBLIC_API_URL||'';
export default function Media(){const [files,setFiles]=useState<any[]>([]); const load=()=>{const t=localStorage.getItem('gd_token');fetch(API+'/api/media',{headers:{Authorization:'Bearer '+t}}).then(r=>r.json()).then(setFiles)};useEffect(load,[]);const up=async(e:any)=>{const f=e.target.files?.[0];if(!f)return;const fd=new FormData();fd.append('file',f);await fetch(API+'/api/media',{method:'POST',headers:{Authorization:'Bearer '+localStorage.getItem('gd_token')},body:fd});load()};return <><h1>Media Library</h1><div className="card"><input type="file" accept="image/*,.png,.jpg,.jpeg" onChange={up}/><p>Admin-only image storage for posters, backgrounds, logos and PNG assets.</p></div><div className="grid" style={{marginTop:16}}>{files.map(f=><div className="card" key={f.id}><img src={API+f.url} style={{width:'100%',height:160,objectFit:'cover',borderRadius:10}}/><b>{f.filename}</b><small>{Math.round(f.size/1024)} KB</small></div>)}</div></>}
